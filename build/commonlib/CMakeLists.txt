CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
SET(PROJECTNAME common)
SET(LIBRARY_OUTPUT_PATH ${ROOT_DIR}/libs)
FILE(GLOB_RECURSE SOURCES "${ROOT_DIR}/commonlib/*.cpp")
ADD_DEFINITIONS("-g -O3 -fPIC")
PROJECT(${PROJECTNAME})

ADD_LIBRARY(${PROJECTNAME} STATIC ${SOURCES})
